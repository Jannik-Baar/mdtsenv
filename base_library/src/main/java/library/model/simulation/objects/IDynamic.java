package library.model.simulation.objects;

/**
 * Interface for everything in the simulation that gets manipulated
 */
public interface IDynamic {

    void addObservedObject(SimulationObject simulationObject);

}
